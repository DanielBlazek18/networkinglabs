{% if 'leaf' in group_names %}
   !
{% for vlan_name, vlan in vlans.items() %}
   vlan {{ vlan.id }}
      rd {{ router_id }}:{{ vlan.id }}
      route-target both 345:10{{ "%04d" | format(vlan.id | int) }}
      redistribute learned
   !
{% endfor %}
{% elif 'spine'in group_names %}
   !
{% endif %}